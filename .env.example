# Server-only credential used by the Vercel Function to call DeepSeek directly.
# Never prefix this key with VITE_.
DEEPSEEK_API_KEY="YOUR_DEEPSEEK_API_KEY"
# Optional: switch the default DeepSeek model without changing application code.
AI_MODEL="deepseek-v4-flash"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="https://todo.ningto.com"

# Supabase integration credentials
VITE_SUPABASE_URL="https://cywbnbvverbdjbbpvsid.supabase.co"
VITE_SUPABASE_ANON_KEY="sb_publishable_VhadPbA-uUCplS280kingw_BUmYmQAQ"
