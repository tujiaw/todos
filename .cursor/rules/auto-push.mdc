---
description: 任务完成后自动提交并推送到远程
alwaysApply: true
---

# 任务完成后推送远程

当一项编码任务完成（功能改完、行为验证无误）后：

1. 提交相关改动（遵循仓库既有 commit message 风格）
2. 立即 `git push` 到当前分支对应的远程
3. 不要等待用户再次要求「提交」或「推送」

注意：

- 不要提交无关文件、密钥、`.env` 等敏感内容
- 不要使用 `--force` / `--no-verify` 等破坏性或跳过钩子的操作，除非用户明确要求
- 若推送失败，向用户说明原因并给出下一步
